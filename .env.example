# Point the Next.js app at your backend (optional).
# If unset, the app uses its own API routes under app/api/.
# NEXT_PUBLIC_API_URL=http://localhost:4000

# Backend (in backend/.env, not here)
# PORT=4000
# GROQ_API_KEY=...
# OPENAI_API_KEY=...
